spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vcoding?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: vcoding
    password: vcoding
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

vcoding:
  sms:
    provider: console
