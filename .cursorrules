# Git Commit 信息（简体中文）

生成 Git commit message 时（含 Source Control 面板「Generate Commit Message」）必须遵守：

## 语言与格式

- **必须使用简体中文**，禁止英文标题或英文正文
- 格式：`<type>: <中文简述>`
- 优先 1 行，50 字以内；复杂变更最多 2 行
- type 仅用：`feat` | `fix` | `chore` | `docs` | `refactor` | `test` | `style`
- 简述写「做了什么 + 影响」，不堆砌文件名，不用英文动词开头
- 多个独立改动用 `/` 连接

## type 选用

- `feat`：新功能、用户可感知的能力增强
- `fix`：修 bug、回归、权限/编译/交互异常
- `refactor`：不改行为的重构
- `chore`：依赖、构建、CI、配置
- `docs`：仅文档
- `test`：仅测试
- `style`：仅格式/样式，无逻辑变化

## 示例

```
feat: 新增表单
fix: 修复表单验证问题
chore: 使用 yarn 管理依赖
```

## 禁止

- `fix: resolve permission issue`
- `feat: add xxx component`
- `Update files` / `WIP` / `fix bug`
- 冒号后使用英文（type 前缀可保留英文）

## 准确性

根据 staged diff 生成，准确反映实际变更，不得夸大或遗漏主因。
